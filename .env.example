# ── PostgreSQL (Azure) ──────────────────────────────────────────
DB_HOST=pgleapwiki-dev.postgres.database.azure.com
DB_PORT=5432
DB_NAME=arc_workbench
DB_USER=arc_workbench_user
DB_PASSWORD=Welcome#2025
DB_SSL=true

# ── JWT ─────────────────────────────────────────────────────────
JWT_SECRET=replace-with-a-long-random-secret

# ── Turso (legacy – can be removed once PostgreSQL is active) ───
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token